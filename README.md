# Armchair QB — Fantasy Football Draft Optimizer

A local draft-day tool built with Streamlit, pandas, and SQLite. It ranks
players by Value Over Replacement (VOR), groups them into tiers, and tracks
your live draft with an always-visible "BEST PICK NOW" recommendation.

## Run it

```bash
pip install -r requirements.txt
streamlit run app.py
